<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="create-bookings-table" author="matchmate-team">

        <createSequence sequenceName="bookings_booking_id_seq" startValue="1" incrementBy="1"/>

        <createTable tableName="bookings">

            <column name="booking_id" type="BIGINT" defaultValueSequenceNext="bookings_booking_id_seq">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"
                             foreignKeyName="fk_bookings_user"
                             references="users(user_id)"/>
            </column>

            <column name="event_id" type="BIGINT">
                <constraints nullable="false"
                             foreignKeyName="fk_bookings_event"
                             references="events(event_id)"/>
            </column>

            <column name="booking_date" type="DATE">
                <constraints nullable="false"/>
            </column>

            <column name="booking_time" type="TIME">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP"/>

        </createTable>
    </changeSet>

    <changeSet id="create-bookings-indexes" author="matchmate-team">

        <createIndex indexName="idx_bookings_user_id" tableName="bookings">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_bookings_event_id" tableName="bookings">
            <column name="event_id"/>
        </createIndex>

        <createIndex indexName="idx_bookings_status" tableName="bookings">
            <column name="status"/>
        </createIndex>

        <createIndex indexName="idx_bookings_user_event" tableName="bookings">
            <column name="user_id"/>
            <column name="event_id"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>